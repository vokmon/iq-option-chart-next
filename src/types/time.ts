/**
 * Time window options for break warning (in minutes)
 */
export type TimeWindow = 15 | 30;

/**
 * Pause duration options for break warning (in minutes)
 */
export type PauseDuration = 15 | 30 | 60;
